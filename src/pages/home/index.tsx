import { Button, Flex, Image, Input, Text, VStack } from "@chakra-ui/react";
import Header from "../../components/NavBar";

export const Home = () => {
  return (
    <Flex flexDir={"column"}>
      <Header />
    </Flex>
  );
};
